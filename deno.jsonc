{
  "tasks": {
    "build": "deno run -A bundle.ts",
    "dev": "deno task build && deno run -A --unstable-cron --watch main.ts",
    "deploy": "deno task build && deno deploy"
  },
  "compilerOptions": {
    "jsx": "react",
    "jsxFactory": "h"
  }
}
